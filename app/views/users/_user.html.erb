<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user_can_delete? user %>
  | <%= link_to t("users.delete.description"), user, data: { "turbo-method": :delete,
                                        turbo_confirm: t("users.delete.confirm") } %>
<% end %>
</li>
